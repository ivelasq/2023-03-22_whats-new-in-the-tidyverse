export interface Completer {
    complete(line: string, pos: number): [string] | null;
}
export declare class IdentityCompleter implements Completer {
    complete(line: string, pos: number): [string] | null;
}
